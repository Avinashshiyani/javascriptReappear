let name = "Avinash";

let lenghtOfName = name.length;
let upperCase = name.toUpperCase();
let lowerCase = name.toLowerCase();
let slice = name.slice(0, 4);
let replace = name.replace("a", "hello");
let concate = name.concat("hello");
// console.log(concate);


for(let i =0 ; i < lenghtOfName ; i++){
    console.log(name[i])
}
